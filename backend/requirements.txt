# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Environment
python-dotenv==1.0.1

# Vector database
chromadb==0.5.0
# SQLite compatibility shim (required on systems with sqlite3 < 3.35.0)
pysqlite3-binary==0.5.2.post3

# Embeddings (dev mode)
sentence-transformers==3.0.1

# PDF parsing (optional, for PDF knowledge base documents)
pypdf==4.3.1

# Word document parsing (optional, for .docx knowledge base documents)
python-docx==1.1.2

# HTTP client
httpx==0.27.0

# AWS Bedrock (production mode)
boto3==1.34.131
